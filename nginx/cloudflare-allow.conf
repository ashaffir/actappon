# /etc/nginx/cloudflare-allow.conf
#
# This file is included inside server blocks. The $from_cloudflare variable is
# calculated from $realip_remote_addr, which is the original socket peer before
# nginx rewrites $remote_addr from CF-Connecting-IP.
if ($from_cloudflare = 0) {
    return 403;
}
